# Deployment (v1.0.0)

Target platform: Ubuntu 22.04/24.04.

High-level steps:

1. Run `./scripts/install.sh`
2. Start services via `./scripts/start.sh`
3. Validate `curl http://127.0.0.1:7890/api/health`
