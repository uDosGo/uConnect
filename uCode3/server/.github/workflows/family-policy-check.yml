name: Family Policy Check

on:
  push:
    branches:
      - develop
      - main
  pull_request:
    branches:
      - develop
      - main
  workflow_dispatch:

jobs:
  family-policy-check:
    uses: fredporter/uDOS-dev/.github/workflows/family-policy-check.yml@main
