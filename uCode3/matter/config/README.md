# Config

`config/` is the checked-in configuration lane for `uHOME-matter`.

Current state:

- `bridge-targets.example.json` provides the starter checked-in target registry
- repo validation is shell and Python standard-library based

Planned use of this root:

- bridge defaults
- Home Assistant adapter presets
- local device-discovery options
