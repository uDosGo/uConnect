# Scripts

`scripts/` stores local validation and developer workflow helpers for
`uHOME-matter`.

Current script surfaces:

- `run-uhome-matter-checks.sh` validates the repo setup spine

Boundary rule:

- keep extension validation here
- keep base runtime behavior in `uHOME-server`
