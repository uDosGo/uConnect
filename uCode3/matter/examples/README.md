# Examples

`examples/` is the smallest operator-facing walkthrough lane for
`uHOME-matter`.

Current example lanes:

- `basic-matter-bridge.json` for the smallest integration contract example
- `basic-home-assistant-clone.json` for the smallest compatible clone profile

Phase 1 rule:

- keep examples aligned to repo-owned extension contracts
- keep runtime implementation behavior in `uHOME-server`
