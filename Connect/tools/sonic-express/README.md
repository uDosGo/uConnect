# `sonic-express`

TypeScript/Node tool for **Sonic** packaging and distribution metadata. **Spawned or invoked** by `@udos/core` where needed.

**Related:** family specs under `uDosDev/docs/specs/v4/` (Sonic, Ventoy, USXD). Historical UI code may live under other repos’ `distro/` trees until migrated.
