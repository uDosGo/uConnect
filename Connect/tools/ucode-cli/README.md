# `ucode-cli` (Go + Bubble Tea) — **VA2**

**Not part of VA1.** VA1 [`udo`](../../core/) is TypeScript-only — no uCode execution, no TUI.

**Role (VA2):** uCode REPL / TUI and sandbox runner — separate process, not the `udo` CLI.

**Status:** stub `main` prints JSON. Port logic from family **`v2-reference/uDOS-shell`** / future `pkg/ucode` when VA2 lands.

```bash
cd tools/ucode-cli && go run .
```
