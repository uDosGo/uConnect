# `ucoin-ledger` (deferred — A2+)

Placeholder for the **SQLite / P2P barter ledger** described in [`modules/ucoin/`](../../modules/ucoin/). Implement as a **standalone binary** with JSON stdin/stdout; **`@udos/core` spawns** it.

**Do not** implement crypto bridges here until intake + policy gates are satisfied (see uCoin specs).
