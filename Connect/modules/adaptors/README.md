# @udos/adaptors

Baseline adaptor schema and tooling for A2.

Current contents:

- `schema/adaptor.schema.json` - initial validation contract

CLI:

- `udo adaptor validate <file>` validates YAML/JSON configs against the baseline rules.
