# `dev/roadmaps/` — template only

**Policy:** Only **`README.md`** and **`TEMPLATE_roadmap.md`** belong in remote. Any other `*.md` here is **gitignored** so local roadmaps and dates do not pollute the shared repo.

Copy **`TEMPLATE_roadmap.md`** when you need a structured roadmap file locally; keep it untracked or move narrative to **`uDosDev/`** if it should be canonical.
