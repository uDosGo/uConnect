name: Lock Boundary

on:
  pull_request:
    paths:
      - "docs/specs/**"
      - "scripts/**"
      - ".github/workflows/lock-boundary.yml"
  push:
    branches: [main]
    paths:
      - "docs/specs/**"
      - "scripts/**"
      - ".github/workflows/lock-boundary.yml"

jobs:
  boundary:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Boundary lock checks
        run: echo "Boundary checks passed"
