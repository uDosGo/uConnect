{
  "name": "@udos/ui-hub",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "uDosConnect UI Hub — index page linking to all uCode surfaces",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.0.3"
  }
}
